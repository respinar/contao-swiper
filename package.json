{
  "name": "contao-swiper",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "cp -r node_modules/swiper/*.js public/js/ && cp -r node_modules/swiper/*.css public/css/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "swiper": "^11.0.5"
  }
}
